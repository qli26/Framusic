<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team8.framusicv2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WizardActivity"
            android:launchMode="singleTask" >
        </activity>
        <activity android:name=".ChoosePicFoldersActivity"
            android:launchMode="singleTask"  >
        </activity>
        <activity android:name=".DisplayBackgroundMusicActivity" 
            android:launchMode="singleTask" >
        </activity>
        <activity android:name=".LayoutSettingActivity" 
            android:launchMode="singleTask" >
        </activity>
        <activity android:name=".MusicSettingActivity" 
            android:launchMode="singleTask" >
        </activity>

    </application>

</manifest>